local schemas = require "kong.dao.schemas_validation"
local plugin_schema = require "kong.plugins.consumer-interval.schema"
local validate_entity = schemas.validate_entity

describe("Plugin: consumer-interval (schema)", function()

end)
